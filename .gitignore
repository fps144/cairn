# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Xcode
*.xcuserdata/
*.xcuserstate
DerivedData/
build/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
Packages/
Package.pins
.swiftpm/
# Package.resolved 纳入版本控制(Cairn 是 App 不是 library,Apple 官方推荐)

# M0.2 起产生的 .app bundle 输出(build/ 已在 Xcode 段匹配,此处为文档化意图)
/build/

# Python (probe)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
.pytest_cache/
.coverage
htmlcov/

# IDE
.idea/
.vscode/

# probe-report.md 已在 commit fd238ca 作为 M0.1 勘察证据提交一次。
# 启用下面这行,防止后续重跑 probe.py 污染工作树。
# 历史版本可通过 `git show fd238ca:probe/probe-report.md` 查看。
/probe/probe-report.md
